discord.py>=2.4.0,<3
python-dotenv>=1.0.1,<2
groq>=0.11.0,<1
openai>=1.50.0,<2
aiohttp>=3.9.0,<4
flask>=3.0.0,<4
# Kept for deployments that serve web.app separately; main.py uses Flask directly.
gunicorn>=22.0.0,<24
# Optional persistence adapter used by the existing repository.
supabase>=2.0.0,<3
